<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.LinearVerticalActivity" />

        <activity android:name=".ui.activity.LinearHorizontalActivity" />

        <activity android:name=".ui.activity.GridVerticalActivity" />

        <activity android:name=".ui.activity.GridHorizontalActivity" />

        <activity android:name=".ui.activity.StaggeredVerticalActivity" />

        <activity android:name=".ui.activity.StaggeredHorizontalActivity" />

        <activity android:name=".ui.activity.RefreshLinearActivity" />

        <activity android:name=".ui.activity.RefreshGridActivity" />

        <activity android:name=".ui.activity.RefreshStaggeredActivity" />

    </application>

</manifest>
